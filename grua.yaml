global:
  project: mq
  volumepath: /var/lib/grua/volumes
openjdk8:
  build: openjdk8
  run: false
tomcat:
  build: tomcat
  options:
    - "--expose=8080"
  volumes:
    - config:/config
    - data:/data
